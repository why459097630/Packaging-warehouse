// NDJC:BLOCK(PLUGINS_EXTRA)
// e.g. id 'com.google.gms.google-services'
// NDJC:END

android {
  namespace "com.ndjc.app"         // ← 已有
  compileSdk 34

  defaultConfig {
    applicationId "com.ndjc.app"
    minSdk 24
    targetSdk 34
    // 版本/包名/渠道锚点
    // NDJC:VERSION_CODE
    // NDJC:VERSION_NAME
    // NDJC:APP_ID_SUFFIX
    // NDJC:RES_CONFIGS      // e.g. 'en', 'zh-CN'
  }

  // 可选签名/CI注入
  // NDJC:SIGNING_CONFIG

  buildTypes {
    release {
      minifyEnabled true
      shrinkResources true
      // NDJC:PROGUARD_KEEP    // 追加 keep 规则文件
      // NDJC:PACKAGING_RULES  // jni/lib 排除
      // NDJC:BUILD_TYPES_EXTRA
    }
    debug {
      // NDJC:DEBUG_OVERRIDES
    }
  }

  // 口味/渠道
  // NDJC:BLOCK(FLAVORS)
  // productFlavors { dev { dimension "env" } prod { dimension "env" } }
  // NDJC:END

  // Java/Kotlin
  // NDJC:COMPILE_OPTS
  // NDJC:KOTLIN_OPTS

  // Compose & 视图相关
  buildFeatures { compose true }
  composeOptions { kotlinCompilerExtensionVersion '1.5.3' }

  // CI 断言（构建期）
  // NDJC:CI_ASSERTS
}

dependencies {
  // NDJC:BLOCK(DEPENDENCIES_EXTRA)
  // implementation "com.squareup.retrofit2:retrofit:2.11.0"
  // implementation "androidx.work:work-runtime-ktx:2.9.1"
  // NDJC:END

  // 测试
  // NDJC:TEST_DEPS
}
