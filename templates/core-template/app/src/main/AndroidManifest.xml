<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- NDJC:INTEGRATION_PERMISSIONS -->
    <!-- <uses-permission android:name="android.permission.INTERNET" /> -->
    <!-- <uses-permission android:name="android.permission.POST_NOTIFICATIONS" /> -->

    <!-- 如需网络明文域或证书固定，可放开下面配置，并在 res/xml/ 提供文件 -->
    <!--
    <application
        android:networkSecurityConfig="@xml/network_security_config" />
    -->

    <application
        android:label="@string/app_name"
        android:icon="@mipmap/ic_launcher"
        android:theme="@style/Theme.NDJC"
        android:extractNativeLibs="true">

        <!-- NDJC:LOCALE_CONFIG -->
        <!-- <meta-data android:name="android.locale_config" android:resource="@xml/locales_config" /> -->

        <!-- NDJC:INTEGRATION_META_DATA -->
        <!-- <meta-data android:name="UMENG_APPKEY" android:value="${UMENG_APPKEY}" /> -->

        <!-- 主入口 -->
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!-- NDJC:INTEGRATION_SCHEMES -->
            <!--
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="ndjc" android:host="open" />
            </intent-filter>
            -->
        </activity>

        <!-- NDJC:INTEGRATION_COMPONENTS -->
        <!-- <service android:name="com.xxx.PushService" android:exported="false" /> -->
        <!-- <receiver android:name="com.xxx.PushReceiver" android:exported="true" /> -->

        <!-- NDJC:INTEGRATION_FILEPROVIDER -->
        <!--
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>
        -->

    </application>
</manifest>
