# Gradle JVM 内存与编码（避免 R8/D8 OOM）
org.gradle.jvmargs=-Xmx4g -Xms512m -XX:MaxMetaspaceSize=1024m -Dfile.encoding=UTF-8 -XX:+UseParallelGC
# 机器内存紧张时限制并发度（可按需调整/删除）
org.gradle.workers.max=2

android.useAndroidX=true
android.enableJetifier=true
