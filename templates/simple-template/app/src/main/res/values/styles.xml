<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- 只保留一个主题，避免与 strings.xml 重复 -->
    <style name="AppTheme" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <item name="android:statusBarColor">@android:color/transparent</item>
    </style>
</resources>
