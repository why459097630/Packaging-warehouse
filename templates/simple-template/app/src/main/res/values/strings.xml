<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- 解决 Manifest 引用 @style/AppTheme 的报错 -->
    <style name="AppTheme" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- 可选，避免状态栏颜色冲突 -->
        <item name="android:statusBarColor">@android:color/transparent</item>
    </style>
</resources>
