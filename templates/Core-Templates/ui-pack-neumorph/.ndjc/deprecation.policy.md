# Deprecation Policy (UI Pack)

当需要改动公开签名时：

1) 在 SST 中**新增**替代项，并将旧项标记 `deprecated`（给出迁移指引与截止日期）。  
2) UI 包同时支持新旧两套，在截止日期前保证 CDC/金样本通过。  
3) 截止日期到达后：移除旧项，并在变更记录中标注 MAJOR 版本提升。

> 未按流程直接改动/删除公开签名 → 视为违约改动，禁止发布。
