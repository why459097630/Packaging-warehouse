
package com.ndjc.feature.demo

import androidx.navigation.NavGraphBuilder
import androidx.navigation.NavHostController
import androidx.navigation.compose.composable

fun registerRoutes(navController: NavHostController) {
    // In a real system, routes would be discovered from module manifests
    // Here we ensure 'home' exists for entry
    // Additional routes are declared in FeatureDemoEntry's NavHost
}
