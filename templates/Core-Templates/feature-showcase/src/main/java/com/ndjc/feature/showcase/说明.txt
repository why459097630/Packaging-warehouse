RestaurantModels.kt —— 数据结构 + 本地存储
showcaseCloudConfig.kt —— Supabase 云端配置常量集中处。
RestaurantViewModel.kt —— 逻辑大脑（业务 + 云端）
showcaseCloudRepository.kt ——Supabase 的数据访问层
ShowcaseUiState.kt  ——把 ViewModel 当前要暴露给壳子/装配层的字段“写死”。

showcaseFeatureAssembly.kt   ——装配点   冻结
showcaseHost.kt   ——VM → Model 映射 冻结
showcaseUiWiring.kt —— 壳子/接线层（把逻辑模块接到 UI 包）。冻结



showcaseUiContract.kt   ——本逻辑模块（showcase/restaurant）的一份“UI 契约文件”。
