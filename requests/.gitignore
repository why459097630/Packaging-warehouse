# 默认忽略 requests 下所有文件
*

# 但保留这些文件
!.gitignore
!.keep

# 先允许遍历子目录（否则下面的白名单无效）
!/**/

# 保留新的结构化日志（生成器/编排器/API 回包/索引）
!**/index.md
!**/meta.json
!**/generator.json
!**/orchestrator.json
!**/api_response.json
